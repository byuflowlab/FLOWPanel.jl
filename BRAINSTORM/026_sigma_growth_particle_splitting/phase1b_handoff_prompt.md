# Handoff prompt — 026 Phase 1b (written 2026-09-04)

Continue BRAINSTORM 026 (particle splitting) — Phase 1b: GPU `euler_exp`, then the RK3 discriminator test. Read `BRAINSTORM/026_sigma_growth_particle_splitting/particle_splitting_design.md` §9 and §12–§14 first (§13 = Phase 1 results/ruling, §14 = the staged plan you are executing), plus `agent_policies/HPC.md` and `agent_policies/WORKFLOW.md` before touching code or the cluster. The integrator's definitive write-up is `BRAINSTORM/020_sigma_aware_subgrid_closure/phase_02r_integrator.md`.

**State:** Phase 1 is COMPLETE and scored (§13). All eight no-split discriminator arms (orc jobs 13569125–132, cases `scr_p026ph1_{ctrl,exp,proj,expproj}_{gpu40,lg}`) ran to full length. Verdict: `WAKE_EXPINT` (euler_exp) alone arrests BOTH independent stretching ignitions (all four exp/expproj arms bounded, max_dtZ ≤ 0.31 < 2/3); SFS no-backscatter projection alone fails both; both ctrls reproduce ignition on CPU (LG at step 504 in-window; gpu40 ~10–15 steps late with identical signature). The §9 binding branch fired: the item redirects to integrator repair; Phase 2 split-geometry work is ON HOLD. Ryan then staged Phase 1b (§14): make euler_exp production-viable on GPU, and falsify/confirm whether RK3's larger stability region suffices.

**Do, in this order:**

1. **GPU/broadcast path for `euler_exp`** (§14 Task 1). Sites: `FLOWVPM_timeintegration.jl:429` (`_euler_exp` scalar `Threads.@threads` loop — frozen-gradient `exp(dt·L)·Γ`, `r^(-3g)`/`r^(-g)` rescale, M[9] Z_eff stash, SFS Lie split) and `FLOWVPM_viscous.jl:175-194` (CoreSpreading euler_exp `expm1` blend using M[9]). Follow the existing `particles isa Array` → scalar / else → broadcast split; templates are `update_particle_states_broadcast_reformulated!` and `_corespreading_euler_broadcast!` (row-slice views + preallocated scratch rows). The 3×3 matrix exponential is the only broadcast-unfriendly piece — a custom CUDA/KernelAbstractions kernel with per-thread StaticArrays is likely simplest (closed-form Cayley–Hamilton/Putzer in broadcast form is the alternative). Invariants: device paths skip `dsigma2_*` splitting accumulators (precedent + comment at `FLOWVPM_timeintegration.jl:383`); `sigma_guard`/`SIGMA_CEIL` stays incompatible with euler_exp; keep (or post-hoc check) the non-finite-ratio DomainError guard. Tests: CPU-vs-GPU parity on a small random field; 020 Phase-2R suite still green (delegate to test-runner); GPU smoke per HPC.md confirming `FLOWVPMCUDAExt`.

2. **RK3 discriminator arms** (§14 Task 2). Prediction on record: RK3's real-axis stability boundary ≈2.51 gives a ΔtZ ceiling ≈0.84 vs forward Euler's 2/3, while the §13 runaway ramps ΔtZ ~0.3→1.3 in ~20 steps → expect DELAY, NOT ARREST; either outcome is decision-grade. Wiring is NOT an env knob: FLOWPanel's `step!` calls `_euler`/`_euler_exp` directly with pre-evaluated U/J (`FLOWPanel_wake.jl:2226-2237`). Use FLOWVPM's `rungekutta3(pfield, dt; custom_UJ)` hook with a closure re-running the wake's particle UJ (+SFS) per stage; recommended and to-be-documented: freeze the panel-surface influence within the step (panel solve happens once), re-evaluate particle velocities/gradients per stage. Add a `WAKE_INTEGRATOR=rk3`-style knob without disturbing `WAKE_EXPINT`. `viscousdiffusion` already has the RK3 per-stage CoreSpreading branch. Launch two arms cloning the §12 pattern in `examples/run_p018_screen_hpc.slurm.sh`: `scr_p026ph1_rk3_gpu40` (RESTART_STEP=950, run to 1100) and `scr_p026ph1_rk3_lg` (RESTART_STEP=450, to 600, add `FLOWPANEL_FILAMENT_REG=linegauss`); `RESTART_NAME=scr_p019_s038v_gpu40`, `RESTART_PATH=data/p026_restart_gpu40_s950` / `data/p026_restart_lg_s450` (both restart dirs already exist on orc), `-p m12 --qos=normal`, CPU host-array; ~3× UJ cost ⇒ ~90 s/step, ~4 h/arm. Verify startup banners (case, reg, restart step, integrator) before leaving the queue unattended. Score exactly as §13 (max_u, max γ/σ², min_σ, max_dtZ trajectories; step where max_u first >100; pre-trigger CT gpu40 950–990 ≈0.072 / LG 450–485 ≈0.0745) and record the result as §15. Delegate log/CSV scraping to harvester and status checks to hpc-monitor.

3. **Report the RK3 verdict to Ryan** (arrest vs delay, with ignition-timing comparison vs ctrl) before any further 026 work.

**Gotchas:** (i) Cluster `~/projects` trees serve live 018/022 campaigns — deploy Phase-1b code via a `git worktree` on orc pointing the launcher's `*_REPO_OVERRIDE`/`*_PROJECT_OVERRIDE` at it; never mutate the live checkouts. Commit (local-only ok) before launching. (ii) `ssh orc` needs the live ControlMaster socket; on 2FA ask Ryan to run `! ssh orc echo ok`. (iii) sacct FAILED is unreliable — judge runs by log step lines + monitor CSV coverage. (iv) The two ignition sources share run name `scr_p019_s038v_gpu40` in different silos; always verify snapshot identity from residue `monitor04_wake_health` CSVs (§12 corrected mapping) — but the extracted restart dirs above are already verified. (v) FLOWPanel bypasses `FLOWVPM.nextstep`; any per-step hook (e.g. `accumulate_H_chi!`, `FLOWPanel_wake.jl:2237`) must be called explicitly in whatever RK3 step path you add. (vi) Never tick notebook checkboxes; notebook writes need Ryan's approval.

**Pending Ryan decisions (surface when convenient, don't act):** archive the eight `scr_p026ph1_*` run dirs via hpc-storage; notebook entry for Phase 0 + Phase 1 (deferred once). Memory entry `project_026_particle_splitting.md` is current as of this handoff; update it with the Phase-1b outcome.
