# Handoff prompt — 026 Phase 1b, scoring leg (written 2026-09-04 ~09:40 local)

Continue BRAINSTORM 026 Phase 1b: harvest and score the two RK3 discriminator arms now running on orc, record the verdict as design doc §15, and report to Ryan. Read `BRAINSTORM/026_sigma_growth_particle_splitting/particle_splitting_design.md` §13–§14 (scoring template + prediction on record), memory `project_026_particle_splitting.md` (Phase 1b section is current as of this handoff), and `agent_policies/HPC.md` before touching the cluster. Delegate CSV scraping to `harvester` and status checks to `hpc-monitor`.

**State (all code work is DONE and committed):**

- **Task 1 (GPU euler_exp) COMPLETE.** FLOWVPM `_euler_exp` now forks CPU/broadcast like `_euler`; the broadcast path evaluates `exp(dt·L)·Γ` matrix-free (substepped order-8 Taylor matvecs within the 11 scratch rows); CoreSpreading euler_exp got its missing `isa Array` fork + broadcast twin. Local commit `8b00dbd` (FLOWVPM, branch `flowpanel`); orc worktree `~/wt026/FLOWVPM.jl` commit `16f9aa5` (ported onto unified-052, which lacks the local splitting-state attribution lines — the port intentionally keeps the cluster's scalar body). Parity testset added to `test/runtests_gpu_fmm.jl` (all green), 020 Phase-2R suite 28/28, and the device smoke `scr_p026ph1b_expgpu_smoke` (orc job 13582150, m13h H200, CuArray pfield, warm-start Vatistas 950→990) **passed gate_rc=0** with wake-health matching the CPU exp arm (max_dtZ≈0.09, γ/σ²≈2e4). Its run dir `data/scr_p026ph1b_expgpu_smoke` is scorable corroboration but not part of the §15 verdict.
- **Task 2 (RK3) wiring COMPLETE, arms RUNNING.** `WAKE_INTEGRATOR=rk3` → `PanelParticleWake(rk3=true)` → `pfield.integration = FLOWVPM.rungekutta3`; `propagate!` calls `_rk3_convect!` (stage 1 reuses the step's evaluated U/J; stages 2–3 re-evaluate particle U/J+SFS at moved positions via the `_make_rk3_stage_UJ` closure — panel SOLVE frozen within the step; `sfs_stage=(a,b)` threaded through the influence hooks keeps DynamicSFS at once-per-step cadence; relaxation uses stage-3 J, documented). FLOWPanel local commits `f4d639f` + `966bb68` (+launcher `cb800e3`/`0495f68`) on `fastmultipole`; orc worktree `~/wt026/FLOWPanel.jl` commits `3537b14`+`f485798`+`c9b411d`.
- **Arms in flight:** orc **13582167** = `scr_p026ph1_rk3_gpu40` (Vatistas, restart 950 → 1100) and **13582168** = `scr_p026ph1_rk3_lg` (LineGauss, restart 450 → 600), m12 `--qos=normal`, 12 h wall, submitted ~08:55 from `~/wt026/FLOWPanel.jl` with `P018_REPO=/home/rander39/wt026/FLOWPanel.jl` (worktree; live checkouts untouched). Both warm-started cleanly (`simulate_warmstart!: resuming from step 950/450`) and are stepping. **Measured pacing: gpu40 ≈300 s/step → ~11.8 h for 141 remaining steps (BRUSHES THE 12 h WALL, may die ~step 1085–1100); LG ≈150 s/step → done ~14:45.** Logs: `~/wt026/FLOWPanel.jl/logs/slurm/slurm-fp-p026ph1-rk3-13582167.{out,err}` and `...rk3-lg-13582168.{out,err}`. Run dirs: `~/projects/FLOWPanel.jl/data/scr_p026ph1_rk3_{gpu40,lg}/`.

**Do, in order:**

1. **Babysit to completion.** (The previous session's monitors died with it — re-arm your own: poll the last step in each run's `monitor04_wake_health` CSV and grep the .err logs for `ERROR|DomainError|OutOfMemory` every ~20 min.) Judge by log step lines + monitor CSV coverage, never sacct. If gpu40 hits the wall short of 1100: steps ≥1070 (the §9 gate) make it fully scorable; if it dies earlier but past ~1040, score what exists and note the truncation; only if Ryan wants the tail, chain a continuation (same sbatch line with `RESTART_STEP=<last saved step> RESTART_NAME=scr_p026ph1_rk3_gpu40 RESTART_PATH=data/scr_p026ph1_rk3_gpu40` — restart mode appends, do NOT wipe the run dir).
2. **Score exactly as §13.** From `data/scr_p026ph1_rk3_*/monitors/scr_p026ph1_rk3_*_monitor04_wake_health_system1.csv`: max_u, max γ/σ² (`max_gamma_over_sigma2`), min σ, max dtZ over the run, and the first step max_u > 100. From `*_CT_vs_rev.csv` use `CT_bernoulli` (the force monitor's CFz is NOT CT): pre-trigger mean CT over gpu40 steps 950–990 (ref ≈0.072) and LG 450–485 (ref ≈0.0745). Reference ctrl ignition: gpu40 u>100 at 1011, LG at 504 (§13 table; ctrl/exp/proj numbers there for comparison).
3. **Rule DELAY vs ARREST** (prediction on record §14: RK3 ceiling ΔtZ≈0.84 vs Euler 2/3 → expect DELAY). Arrest = bounded max γ/σ², no min_σ collapse to the 9.41e-5 floor, no compounding leader, max_dtZ staying < ~0.84, to end of run. Delay = same ignition signature at a later step. Record the result as design doc **§15** (follow §13's format; include the pacing/cost observation: RK3 measured ~5–10× the euler arms' 29 s/step, well above the 3×-UJ estimate — the stage re-evaluations rebuild FMM trees and panel influence per stage). Report the verdict to Ryan with ignition-timing comparison vs ctrl BEFORE any further 026 work. Then update memory `project_026_particle_splitting.md` (its Phase-1b section is written expecting exactly this).

4. **After the §15 report: hunt for blowup examples where the exponential integrator does NOT help** (Ryan 2026-09-04). euler_exp only removes the frozen-gradient ΔtZ-overshoot mechanism; the goal is a test case (ideally a warm-startable event, §12-style) where an expint arm still blows up, to map the limits of integrator repair. Leads, cheapest first:
   - **BRAINSTORM 020 Phase 2 expint run** — already documented as "zero neg-σ, floor held, wake still died physically" (memory `project_020_sigma_closure.md`; entry = that item's "Current status"). This may already BE the example: pull its wake-health/monitor residue and characterize how it died (σ-growth side? SFS? resolution loss?) before hunting anything new.
   - **Ignitions with max_dtZ < 2/3 at blowup**: sweep the `monitor04_wake_health` CSV residue of archived/failed runs (login node; residue stays on /home) for runs where max_u ignites while max_dtZ never crosses ~0.5 — those are by construction not Euler-overshoot events and expint shouldn't save them. Candidates to check: `p022lg_hr10` (022 ground-effect corpse, ignition ~step 710), the 018 NT144 σ-cap cliff event (compression/growth side — note `sigma_guard`/`SIGMA_CEIL` is *incompatible* with euler_exp, so growth-side cliffs are outside its protection by design), and anything the W6 census (`scripts/p026_sigma_cap_census.jl`) flagged as rVPM-compression-dominated.
   - **Mechanism arguments for where expint must fail** (use to design a synthetic case if no archived event fits): rapidly *rotating* strain axis within a step (frozen-L error, not overshoot); pair-collapse where the gradient stiffens faster than once-per-step evaluation can track (the §9 patient-zero feedback loop but at smaller separation); SFS/Cd flip-flop amplification (proj arms showed backscatter removal is not the lever, but Cd oscillation remains unmodeled); and pure resolution-loss (overlap) failures — the original 026 splitting motivation, which no local integrator can fix.
   - Deliverable: a shortlist (event, silo/tarball, ignition window, max_dtZ at ignition, why expint shouldn't help), then — with Ryan's go — one §12-style discriminator pair (ctrl vs `WAKE_EXPINT=true`) on the best candidate. An expint-fails event becomes the validation target that re-motivates Phase 2 splitting.

**Gotchas (all live):**

- `ssh orc` needs the ControlMaster socket; on 2FA ask Ryan to run `! ssh orc echo ok`.
- **The archiver ATE the restart dirs once already** (pass at 2026-09-04 05:33Z stripped `data/p026_restart_{gpu40_s950,lg_s450}`); they were restored with `./scripts/run_archiver.sh --root /home/rander39/projects/FLOWPanel.jl --restore <name> --apply` (login node only). If anything needs those states again, check for empty `*_body1/` dirs first. Suggest to Ryan protect-listing `p026_restart_*` and `scr_p026ph1*` (the protect list is HIS file — never edit it).
- The worktree needed `data → ~/projects/FLOWPanel.jl/data` symlink AND 27 symlinked untracked `examples/data/` assets (meshes); already done. `~/wt026/env-gpu` is the GPU env (resolved under module `julia/1.11.7-6bmogfl`, NOT juliaup 1.12 — a 1.12 resolve breaks jll precompile on 1.11).
- The cluster launcher has no `RESTART MODE` echo (that's a local-only block); the driver reads `RESTART_*` env directly — the warm-start sentinel is `simulate_warmstart!: resuming from step N` in the .out. A false-alarm scancel already wasted one wave; don't repeat it.
- FLOWPanel bypasses `FLOWVPM.nextstep`; `accumulate_H_chi!` runs after the integrator branch in `propagate!` — already handled for rk3.
- Never tick notebook checkboxes; notebook writes need Ryan's approval.

**Pending Ryan decisions (surface when convenient, don't act):** archive the eight Phase-1 `scr_p026ph1_*` run dirs + the two rk3 dirs + the smoke dir via hpc-storage once scored; notebook entry for Phase 0 + Phase 1 (+1b) — deferred once already; protect-listing the p026 restart dirs; whether to keep or delete the `~/wt026` worktrees + `~/wt026/env-gpu` after Phase 1b closes (local branches hold the canonical commits); whether the RK3 wiring should merge to the mainline branches regardless of verdict (it's benign: default-off knob, sfs_stage default (1,1) is behavior-identical).
