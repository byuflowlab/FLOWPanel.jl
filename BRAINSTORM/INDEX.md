# Brainstorm Items for Consideration

This file contains a concise table of all .md files in this directory and their subject matter. Details of each item and a log of its progress should be kept in the referenced .md file itself. This file is meant to be scannable, and include only high-level summaries and status (1 sentence per item).

## Mission Statement

These items should lead to validation of FLOWPanel for the rotor hover $C_T$ case.

## Agent Workflow

Agents should begin by reading this file to understand how to navigate the brainstorm items and their approval process. When an agent is assigned to work on a specific item, they should read that item's .md file for detailed context, proposed path, acceptance criteria, and any caveats. The agent should then carry out the investigation or implementation work as described in the item, keeping detailed notes and data in the item's .md file.

## Progress Summary

Use the approval columns as gates for each brainstorm item:

- **Technical Completion**: Check this only after the investigation or implementation work described by the item is complete enough to evaluate against its own acceptance criteria.
- **Clear-Context Approval**: Check this only when an agent with clear context can approve the item immediately as satisfying its requirements. Conditional approval is not allowed: if approval depends on further fixes, clarification, or follow-up work, leave this unchecked and treat the item as still being in the technical completion phase. Approval should seek ways to improve the item, considering the following in order: 1) relevance to the mission statement; 2) correctness of implementation and conclusions; 3) clarity and conciseness of presentation.
- **User Approval After AI Discussion**: Check this only after the user has discussed the item with another clear-context AI agent and explicitly approved the item or its conclusion.
- **Outcome**: Record only a *very concise* summary here (headline conclusion + status; complete sentences not needed). Keep the INDEX scannable — put all detail (data tables, run commands, derivations, recommendations) in appended dated sections inside the item's own `.md` file, and reference that file rather than expanding the Outcome cell.

| File | Subject | Technical Completion | Clear-Context Approval | User Approval After AI Discussion | Outcome |
| --- | --- | --- | --- | --- | --- |
| `001_rotor_hover_low_effort_checks.md` | Low-effort implementation checks for the rotor-hover CT discrepancy: backend agreement, force-method agreement, Kutta-condition sensitivity, and semi-infinite rigid-wake smoke test. | [x] | [ ] | [ ] | Audit via `examples/rotor_hover_force_method_audit.jl` (dji9443 40_40, 5400 RPM, steady rigid wake). **Check 1 backend:** Direct vs FMM agree to 0.000% on the same solved state — no backend error. **Check 3 Kutta:** negligible (Bernoulli Δ0.4%, vorticity Δ0.0%) — not the cause. **Check 2 force method:** 16.3% spread on the SAME state (Bernoulli 0.0505 / Laplace 0.0520 / SurfaceVorticity 0.0603 / KJ 0.0506); surface-vorticity sits closest to the particle-wake 0.062 — a real same-state disagreement worth a follow-up. **Check 4 semi-infinite rigid wake:** materially moves CT — doubles it (Bernoulli 0.0505→0.110, +118%), overshooting the 0.068–0.072 reference. **Verdict:** track survives on the force-method spread, but the dominant lever is clearly wake modeling (Check 4); backend and Kutta are cleared. Focus shifts to wake physics (002/004), consistent with 003 (BEM=0.068). Setup gotcha: shedding must be derived from the watertight-reoriented `RigidWakeBody.cells`, not raw mesh cells, or CT collapses to ~0.014. |
| `002_rotor_hover_prescribed_helical_wake.md` | Diagnostic finite panel helical-wake idea with TE-strip strengths and pitch iteration from azimuthally averaged inflow. | [ ] | [ ] | [ ] | Still open. Seeded short wakes produce plausible CT but geometry does not converge. One-/two-row tests confirm the capped Picard geometry update is the blocker (`max_target_residual_R` 0.4168/0.5108, cap fraction 1.0, strength update ~13.8-14.0%). Added high-volume `DEBUG_VTK_EVERY_ITER` dumps for visual inspection. Next: replace age-marched capped Picard target with a residual-minimizing wake-shape solver before longer wakes or more outer iterations. |
| `003_rotor_hover_ccblade_bem_comparison.md` | CCBlade/BEM cross-check plan to separate missing wake physics from geometry, airfoil, Reynolds-number, or operating-condition assumptions. | [x] | [ ] | [ ] | CCBlade BEM (same DJI9443 CST geometry, RPM=5400, ρ=1.179, XFOIL polars at local Re~10k–54k/M) gives hover CT≈0.060–0.071 vs experiment 0.072 and panel 0.0506. XFOIL ncrit sensitivity (low-Re transition): ncrit=1→0.0711 (99% exp), 4→0.0705 (98%), 9→0.0683 (95%), 14→0.0598 (83%) — lower ncrit (noisier inflow, physical for a rotor ingesting its own wake) raises CT; ncrit=1/4 nearly exact (saturating below 4). Lift polars used are plotted (`polars_cl_ncrit{N}.png`, `polars_cl_vs_ncrit.png`): higher ncrit shows low-Re laminar-separation kinks / lower clmax. Across the plausible transition range BEM brackets experiment and stays 18–41% above the panel using identical geometry/airfoil/Re inputs, so the panel shortfall routes to **wake / induced-velocity modeling**, not geometry/airfoil/Re/operating-condition assumptions. See `examples/rotor_hover_ccblade.jl` (NCRIT_LIST default [1,4,9,14]), outputs in `data/rotor_hover_ccblade/`. |
| `004_rotor_hover_wake_decay_audit.md` | Audit plan for wake-bearing runs that pass through `CT ≈ 0.072` and later drift lower, focused on numerical wake-modeling mechanisms and transient behavior. | [ ] | [ ] | [ ] | TBD |
| `005_rotor_hover_stable_converged_particle_wake.md` | Constructive plan targeting **stable, non-oscillating** particle-wake thrust (a flat history at any value is a win — magnitude is 002/003/004's job) by combining existing features in `examples/rotor_hover_pressure_comparison.jl`: gradual RPM spin-up, a ramped-up-then-withdrawn convecting freestream to develop a self-sustaining wake column, and downstream particle truncation for cost. Ramp rates require intelligent, iterative tuning. | [x] | [x] | [x] | APPROVED. Built staged-startup machinery (4-phase smoothstep freestream pulse + ENV hooks `MAGVINF_PEAK/END`, `FREESTREAM_RAMP/HOLD/WITHDRAW_REVS`, `SETTLE_REVS`, `TRUNCATION_DEPTH_R`, `BERNOULLI_ONLY`; run-length bookkeeping; plateau diagnostics) + driver `run_rotor_hover_stable_wake.sh`. **Staged startup KILLS the impulsive startup ring** (baseline CT 0.11 overshoot + ~6-rev ring → smooth flat ~0.055 band by rev 1.3 while freestream held). **Phase 2 (2026-06-19) characterized the post-withdrawal signal and corrected the earlier "intrinsic/non-damping/growing" overcall:** E1 long run (48 cycles) shows a **bounded, STEADY 2/rev limit cycle** (period 0.498±0.021 rev, growth≈0) on a flat ~0.0845 plateau — non-damping but NOT growing. E2 depth gate (1/2/4) shows the big `iter4` ripple was largely a **near-disk truncation artifact**: amp shrinks ~5× and period scales with depth ⇒ depth=1 untrustworthy, run at ≥4R. E3: slower withdrawal halves the ripple; staged pulse is beneficial (pure-spinup is worse); **best baseline = depth=4 + withdraw_12** (detrended ptp 0.0042, mean CT ~0.060). E4 13-scheme damping menu on that baseline: residual ripple already ~4% of CT; **no add-on damper reduces it** (all within noise or harmful). Tested resolution refinements sharpen/worsen the residual 2/rev mode, supporting a robust blade-passage-scale wake response rather than an obvious under-resolution artifact. The physical config (truncation depth + withdrawal gradualness) is the real lever; numerical dampers (incl. new E4.8 `BOUND_STRENGTH_RLX` body-strength low-pass, default off) add nothing here. Tooling: `examples/analyze_stable_wake_oscillation.jl`. Repro data exists off-machine; detail in item's 2026-06-19 section. |
| `006_rotor_hover_converge_stable_near_reference.md` | Convergence track: drive the particle-wake CT history to be **both** stable (non-oscillating) **and** near the 0.068–0.072 reference at validated loose truncation (≥4R). Unlike 005 (any flat value), 006 also requires magnitude. **Gated on 005 Phase 2 (E1–E4)**, whose results decide 006's lever set (truncation depth, ramp/withdraw rate, VPM relaxation scheme — needs a new corrected-Pedrizzetti/rlxf/nsteps_relax ENV hook — plus viscous/SFS/resolution). 60-thread budget, OMP/BLAS tied to per-run threads. Temporal+grid convergence split to a future item (007). | [ ] | [ ] | [ ] | Open — **PARTIAL: magnitude half MET, stability half NOT.** The magnitude lever turned out to be the **first-wake-row offset `Das`/η**, hard-coded at η=0.2 from the beginning and inherited by every prior number. On the production mesh (5400 RPM, NT=36, 4R, unfiltered) the 10-rev cycle-mean goes **0.06148 (η=0.2) → 0.07133 ± 0.00159 (η=1.0) → 0.07190 (η=4.0)** — **+16.0%, inside the 0.068–0.072 band**, flat for η≥1 (+0.8%, < scatter). **This reverses the 2026-07-27 verdict**: the gap was neither wake resolution nor mesh refinement. Study B also answered positively — shallow filters (0.5R→0.0662, 1.0R→0.0658) both COMPLETE where 2.0R diverges, so that instability is a *depth* effect. **Stability half unmet:** η=1.0 ripple is 2.23% / 7.15% p-p vs tolerances 0.5% / 2% — a limit cycle at the right level. Remaining lever = the **combination** (η=1.0 + 0.5R filter, which has the lowest ripple measured here; job 12921072 in flight), with the caveat it may overshoot to ~0.076 if additive. **Do NOT read 0.0713 as settled physics:** the mechanism is unexplained, three candidate explanations were refuted, and the η-selection principle is now open item **014** — whose `min_displacement`-floor confound (floor, not η, sets `Das` over ~68% of the span at η=0.2) could reframe the attribution. Also in flight: NT=72 at η=1.0 (12921559) and the arc-`Das` A/B (12921071). Detail in the 2026-07-28 sections. |
| `008_particle_overlap_vorticity_evolution.md` | First pose the overlap-aware particle-strength linear system for generic kernel `ζ`, then specialize it to FLOWVPM/FLOWPanel particle-wake updates. | [ ] | [ ] | [ ] | Theory phase done. 2026-06-24: built offline collocation mat-vec residual diagnostic (`examples/particle_overlap_residual{,_diag}.jl`) over saved VTP states; gates pass (no-overlap 1e-16; matvec-vs-bruteforce 4e-6; kernel calib vs curl-of-J 0.19 since saved `vorticity` is empty). On `rotor_hover_pressure_comparison` (20 settled states) the result is STABLE: heavy overlap (~22.5k nbrs, diag-dominance median 0.006), and the live FLOWVPM single-particle update sits ~62% (sampled fvpm residual) from the overlap-projected RHS — **GO**: an overlap solve would materially change the live update. Convective term enlarges (not explains) the gap; phys-vs-fvpm gap is a separate transposed-reformulation effect. Next (not done): Gaussian-overlap Galerkin (SPD) CG prototype, then full-RHS/SFS and Pedrizzetti comparison; 2nd target run pending from user. |
| `009_particle_field_divergence_free.md` | Theory + ForwardDiff numerical check of whether the vortex-particle-induced velocity field is divergence-free, at particle centers and random points for heavy/light/no overlap. | [x] | [x] | [x] | APPROVED. **Divergence-free confirmed.** Theory: each radial particle kernel `K=f(\|r\|)r` is curl-free, so `u_p=K×Γ` has `∇·u_p=0` exactly for any regularization/overlap (velocity = curl of a mollified vector potential). ForwardDiff through the Gaussian-erf kernel (`examples/particle_divergence_free_check.jl`) gives relative `tr(J)` ~1e-11 (round-off) for d=0.25σ/2σ/20σ at centers + 400 random pts, while curl is O(1e-2) (≫div) and a non-radial broken control gives rel-div ~1 (metric has teeth). AD at centers needs the `ρ²` Taylor branch (naive `g/r³` is `0/0`). Validates the incompressibility assumption in `PressureLaplace`/unsteady-Bernoulli/008. |
| `010_particle_vorticity_curl_vs_basis.md` | Theory + single-particle ForwardDiff numerical check comparing particle vorticity from `curl(velocity_gradient)` against direct `ζ`-basis evaluation, with root-cause analysis and self-consistency improvement options. | [x] | [ ] | [ ] | N=1 synthetic check passes (`examples/particle_vorticity_curl_vs_basis_check.jl`): center discrepancy `\|\omega_J-\omega_\zeta\|/\|\omega_\zeta\| = 1/3` to Float64 precision, `\delta` matches `Hess(G_σ)\Gamma` to `6.3e-13`, and `∇·ω_J` is roundoff (`1.3e-16`). Confirms curl-of-J is the projected vorticity with the `2/3` self-term, not a `ζ` normalization/sign artifact. **M1 (divergence-correction term) explored**: closed-form analytic `Hess(G_σ)Γ = A·Γ + B·(y·Γ)·y` kernel (no ForwardDiff/grid, O(N)/FMM-able) added to `examples/particle_vorticity_curl_vs_basis_check.jl` and matched to ForwardDiff to `~8e-13` (`M1_SELFTEST=1`); diagnostic/physics-term role, no strength change. Self-vs-overlap numerical decomposition deferred. **M2 (effective strength Γ_eff) explored**: `Γ̇_eff` evolution law *derived* (project `Dω/Dt=(ω·∇)u` onto ζ basis ⟹ `M Γ̇_eff=b_eff` with `ω_J=MΓ_eff` stretched; self-limit `Γ̇_eff,k=J_kΓ_eff,k`, cheaper than Pedrizzetti) and the `Γ_eff=Γ+M⁻¹HΓ` projection *implemented* + self-tested (`M2_SELFTEST=1`: 2/3 self-limit to 1e-16, CG-vs-dense 1e-16, off-node field 8.6× closer to ω_J with nonzero best-fit floor). **M4 docs/theory advanced + self-tested** (`M4_SELFTEST=1`): `K_σΓ=ζ_σΓ+Hess(G_σ)Γ` matches curl(u) to `1.8e-14`, is divergence-free to `7.6e-14`, preserves Gaussian diffusion compatibility, but has a real nonlocal moment caveat (`∫K_σ` naive finite-volume check tends `2/3Γ`, not `Γ`). **M12 resolved at theory level**: local/L1 exact-div-free kernels necessarily have zero `∫ω` moment; physical circulation is loop circulation / vorticity flux, so the viable path is velocity-primary pairing (`ω=∇×u`) with dynamics still owned by M6. **M6 carried out (2026-06-25):** projected-basis block-kernel `Γ̇` equation derived; no-overlap reduction `Γ̇_k=J_kΓ_k`, viscous `∂_σK` cancellation, and `1/r³`-tail block coupling all N=1 self-tested (`M6_SELFTEST=1`); saved-state diagnostic (`examples/particle_m6_projected_basis_diag.jl`, 20 settled states) is the **negative branch** — the `3×3`-block overlap operator is ~20× *more* ill-conditioned than the already near-singular scalar M (cond ~9e14 vs 4.6e13, block diag-dominance ~0.01), so neither truncated nor global block solve is a usable live update; binding constraint is heavy overlap (~21k nbrs), not the basis. Technical completion: all required tracks (a)+(b) M1/M2/M4/M6/M12 done with self-tests. |
| `011_pedrizzetti_overlap_residual_efficacy.md` | Quantify how much of the 008/M2/M6 overlap-aware strength correction the cheap, already-shipped Pedrizzetti relaxation captures on its own — gating whether an explicit CG/block overlap solve is worth its cost. | [x] | [ ] | [ ] | **Pedrizzetti does NOT substitute for the overlap solve.** Offline diagnostic (`examples/particle_pedrizzetti_overlap_diag.jl`, 20 settled states, gates pass). At the relaxed fixed point `Γ` is already aligned with `ω_J` (median angle **1.1°**) — direction job converged — but the leftover overlap correction is **~98% magnitude (∥Γ)**, which norm-preserving Pedrizzetti structurally cannot reach; its rotation is nearly **orthogonal** to the overlap-solve correction (`cos(d_pedr,d_eff)≈0.03`, moves `Γ` no closer to local `Γ_eff`); marginal Pedrizzetti rate increases the 008 residual (~6.6×, rate-sensitive). Verdict: the unaddressed content is **magnitude**, but M6 showed that solve is ill-conditioned here ⟹ lever is overlap reduction (012). Overlap re-characterized: TRUE local **`σ/h≈4.2`**, **~394 within 2σ_local / ~181 kernel-effective** (the "~21k" was a `4·σ_max` cutoff artifact spanning ~1.7R). Live relax on/off + `C_T` A/B = optional follow-up. |
| `013_unsteady_wake_validation_cases.md` | Web-search survey of published validation cases for an unsteady inviscid panel method that would show off a shed free wake (VPM or doublet-panel), prioritizing cases with both existing panel-code AND experimental data. | [x] | [ ] | [ ] | Five ranked candidates. **(1) Heathcote/Wang/Gursul heaving rectangular NACA0012 wing** — 3D flapping, thrust/eff vs Strouhal, exp + FLOW-ecosystem VPM curves (top pick, both). **(2) Anderson et al. 1998 heave+pitch foil** — reverse Kármán, exp + inviscid panel theory in-paper (both). **(3) Wagner/Theodorsen** — exact analytic shed-wake sanity check, do first (theory only). **(4) Caradonna–Tung hover rotor** — mission tie-in, exp + panel/VVPM (both; overlaps 002–006). **(5) AGARD 445.6 flutter wing** — exp + unsteady panel free-wake (both; needs aeroelastic coupling, stretch). Suggested order: 3→1→2→4→5. **Expanded 2026-07-21 with 5 genuinely-3D cases** (incompressible/high-Re/attached/no-stall-no-LEV, ≥1 matching case per paper): **(6) isolated propeller axial flight** — 3D, helical wake, reuses `rotor_axial`, exp+free-wake (lowest-effort 3D pick); **(7) MEXICO wind-turbine rotor** — exp (pressures+PIV tip vortex) + free-wake *unsteady panel* (closest literature match; low-TSR stall = in-paper violating case); **(8) propeller–wing slipstream** — convecting wake over a wing, exp+free-wake; **(9) wing–gust family** with explicit 2D-vs-3D distinction — **9a UMD transverse gust** = only genuine 3D finite wing (AR≈1/2/4 sweep, open downloadable data via Towne et al. arXiv:2206.11801); **9b Sears sinusoidal** + **9c Küssner sharp-edged** = 2D-airfoil analytic verification rungs (open/public-domain: MDPI Fluids, NASA NTRS, NACA gust-tunnel); **(10) 3D sudden-start finite wing** — 3D-Wagner verification, code-to-code only. **Re>200k screen (2026-07-21):** added **(11) marine propeller open-water** (Re≈5e5–1e6, *exactly incompressible* in water, open KT/KQ — Wageningen B-series/DTMB 4119/PPTC — + free-wake panel; strongest high-Re fit); existing #4 Caradonna–Tung (low M_tip, Re≈1.9e6), #7 MEXICO (Re≈3–6e5), #6 full-scale prop also clear; #1/#2/#9 fail (water-tunnel Re≈1e4–1e5). High-Re picks: 11→4→7→6. Documentation-only; no implementation yet. |
| `012_robust_resolution_overlap_management.md` | Explore smarter, robust particle-resolution management that bounds the overlap ratio `σ/h` (measured ≈4.2 in the settled wake, ~4× nominal; ~394 within 2σ_local, ~181 kernel-effective — the "~21k" was a 4·σ_max cutoff artifact) while preserving vortical structure — potentially dissolving the conditioning, divergence-discrepancy, and Pedrizzetti-workload problems at their shared source. | [ ] | [ ] | [ ] | Staged. All of 008/010/011 pivot on heavy overlap; existing machinery works at low overlap. Plan: characterize `σ/h` over wake/time (CSR grid), audit current merging/core-reset triggers (`FLOWVPM_merging.jl`, `particles_merging_savings.csv`), design overlap-bounded moment-conserving merging (0th circulation + 1st impulse), validate offline then live with `C_T`/peak-vorticity preservation as first-class. Highest physics coupling (smearing tip vortices changes `C_T`); stage behind 011 + M6 consumer audit. |
| `014_first_wake_row_offset_selection.md` | **OPEN QUESTION:** on what principle should the first-wake-row offset `Das`/η be chosen, so that CT is a converged prediction rather than a tuned number? | [ ] | [ ] | [ ] | **FINITE-AR WAGNER ORACLE (`examples/ssw_eta_convergence.jl`), RE-RUN 2026-07-29 ON THE CORRECTED MESH — all findings stand, numbers moved <1%: η is the WRONG parameter — the effect collapses on the physical length `Das`=η·dt·U; fixed-`Das` dt-refinement converges cleanly (successive ratios 0.39–0.47), fixed-η has NO dt→0 limit — the tail η-spread is dt-INVARIANT (2.637% at dt*=1/4 vs 2.692% at dt*=1/32), which is the sharpest refutation of η-as-truncation-error; dt-converged lift grows logarithmically in `Das` at +0.205%/doubling (RMS resid 0.0035%), unsaturated out to 4c.** Corrected a loose earlier statement: the log law holds in the dt-CONVERGED limit; along a fixed-dt row the per-doubling increment runs 1.11%→0.21% because η (hence dt error) varies along it. Draft rule: pick `Das` as a physical length on the log plateau (≈0.25–1.5 local chords; frozen rotor η=1 qualifies) and carry ±~1% model-form uncertainty — NOT ±16%: the rotor's +37% is ~77x the log law (predicted +0.48% over the 2.36 Das-doublings of eta 0.2->1.0), so a rotor-specific amplifier (floor / `nwakerows=1` handoff / σ) is still required; staged HPC discriminators `p2e_das0p2_nofloor`, `p2e_nrows2_dassmall`, `p2e_nrows4_das1p0`, `p2e_das1p0_refresh` are wired (driver `NWAKEROWS`/`DAS_REFRESH` env + `set_Das_refresh` in `simulate!`, unit-tested) but NOT yet submitted. 2026-07-29 rotor discriminators: floor was PROPPING CT up (nofloor η=0.2 → 0.0522), total-handoff hypothesis dead (0.0336), nrows4 +4.2% (0.0743) ⇒ sheet/particle split governs. **Wing ladder (Ryan-requested, `examples/ssw_sheet_particle_split.jl`): particles ≈ sheets to <0.1% at wing σ; at rotor-like σ/c=1.5 the split moves lift ±3% with a sign flip ⇒ σ/chord is the bridge variable — routes to 012 (σ management), `Das` itself settled (physical length on log plateau, ±≲1%).** 2026-07-30: nrows ladder NON-MONOTONE (1→0.0713, 8→0.0751, 36→0.0705; peak N=8) — not convergent; **Ryan ruling: N=36 NOT adopted; next = targeted straight-wing steady study isolating the sheet/particle variability cause (induced-velocity-level, rotor σ/chord)** — see 014 "Ruling 2026-07-30", which also tables the in-flight jobs for harvest (12955430 nrows72, 12943696 sigF_nofilt, 12950996 nt72match). Naive NT=72 → 0.0734 (outside log-law pre-registration; confounded — matched run isolates dt). Zero-Γ particle NaN bug found+fixed in `_shed_particles!`. Old η ladder + Ryan's proposals + refuted mechanisms retained in the file. Also: AR=100 SSW baseline confirmed broken (CLsteady=1.4e5) — finite-AR (Jones NACA-681 reference) is the validated oracle now. η is the **largest discretization sensitivity found anywhere in this study**: η 0.2→1.0 gives **+16.0% CT** (0.06148→0.07133), vs <scatter for mesh refinement, Green, truncation and relaxation strength. η=0.2 was hard-coded from the start and inherited by every prior rotor number; changing it moved CT from ~15% below experiment into 006's band — so **if η is free, 0.0713 is tuned, not predicted.** Not rotor-specific (pitching-wing oracle: +13% over 4× `das`). ESTABLISHED: the panel/particle partition tiles space cleanly at ANY η (panel on [TE,TE+Das] per `elements_fmm.jl:1044-1066`, `PanelWake` row 1 exactly at TE+Das per `simulate.jl:1021`), so nothing internal selects a value; `Das` is **frozen at its t=0 40%-RPM magnitude** (η_eff = 0.4η); CT flat for η≥1 (+0.8%); measured σ ≈ **1.5 local chords** at shedding, `Das`/σ = 0.267η. **THREE mechanisms proposed and ALL REFUTED** (near-wake-pushed-away; particles-inside-panel-span; core-straddles-TE) — the +16% has no surviving explanation. **Ryan 2026-07-28 — the decisive experiment:** at the hard-coded `nwakerows=1`, *rigid-sheet length* and *particle-handoff distance* are THE SAME NUMBER, which is why the +16% resists explanation; raising `nwakerows` (free convecting rows) separates them — trade smaller `Das` against more rows at fixed total handoff distance and see if they converge to the same CT. Also reframed: **`Das` exists only to give the Kutta condition a finite wake geometry**, so the real question may be whether a Kutta enforcement exists that needs no `Das` at all. **NEW CONFOUND FOUND:** `min_displacement` (hard-coded 0.01R) clamps `Das` over **68% of the span at η=0.2** vs 3.6% at η=1.0, so the ladder was never a clean single-parameter sweep — cheapest test is η=0.2 with the floor off. **NEXT PLAN READY: `plans/20260729_wake_representation_consistency.md`** (self-contained; leads with a wake-representation consistency check, since a steady uniform-Γ `Das` sweep is a theorem — constant-strength doublet panels have exactly cancelling interior edges). Floor test and `nwakerows` DONE: floor-free η=0.2 → 0.05215 (so the true sensitivity is **+37%**, not +16%); `nwakerows` 1→4 at fixed `Das` → +4.2% (free rows not inert, but `Das` dominates ~10×). `das1p0_refresh` CANCELLED-INVALID (`Backslash` factorized G with `Das`=0). Only 12921559 (NT=72, η=1.0) still running. **WAKE-REPRESENTATION CONSISTENCY 2026-07-29: CONVERGED — hypothesis ELIMINATED.** New `PanelWake` `freestream_convection` mode (all rows convect with freestream only ⇒ geometry identical to the semi-infinite wake) converges to the semi-infinite reference monotonically, rel err −19.5% → **−0.014%** over `L/c` 1→64, asymptotic order ≈2.2 (the `1/L²` starting-vortex-loop closure); rolled-up control correctly plateaus at −0.96%; AR=3 reproduces it. The code's two wake representations agree, so a representation inconsistency is NOT a candidate for the +37% — and the η ladder's standing presumption is now verified rather than assumed. §6 diagnostic ladder was gated on non-convergence and is NOT triggered. **Two topological mesh defects found and fixed en route** (symmetric node cloud, asymmetric triangulation): TE panels were not chordwise reflections (upper/lower TE control points 0.8% chord apart), and **all 480 cells violated y→−y symmetry, giving ~10% spanwise Γ asymmetry on a symmetric wing that did NOT refine away** (n_span 12/24/48); fixed via XOR of the chordwise+spanwise half-tests, now 1.3e-14, guarded by `assert_ssw_mesh_symmetry`. F1–F4 and the `Das` log law shared this mesh and **have now been fully re-run on it (2026-07-29); all stand.** `SSW_MESH_REVISION` is now carried in the case tag so a mesh change cannot silently resume stale histories. Data `data/suddenly_started_wing/wake_consistency.csv`. Next: the un-gated §7 experiment — sweep `Das` at fixed LARGE `nwakerows` (needs η=0.2 at nrows=4 to pair with `p2e_nrows4_das1p0`); then the untested FMM-radius-vs-`Das` coupling; then un-freeze `Das` properly. Negative outcome acceptable: if η is irreducibly a model parameter, ±16% becomes a model-form uncertainty on every reported CT. |
| `015_pressure_continuity_kutta_condition.md` | Gated theory, architecture, implementation, and validation of independently configurable wake attachment and Kutta closure for paired-edge Dirichlet lifting bodies. | [ ] | [ ] | [ ] | **Phase 4 V1 still BLOCKED at the wing sanity gate; the gap is now ATTRIBUTED to a formulation-internal cause.** Gate rebuilt as a scripted 6-cell battery (settled t*∈[7,8]). Battery I (44 cells) showed the Dirichlet-vs-Neumann circulation gap falls monotonically with the first-wake-row offset η (`Das = η·Δt·U`), 20.3%→3.9% over η=0.0625→4 against a 3.0% semi-infinite anchor, and was reported as "η is the dominant lever". **Battery II (21 cells, `KUTTAV1_STAGE=gate_diagnosis2`) corrects that attribution**: measured against each body's OWN anchor, both bodies lose circulation as Das→0 but the Dirichlet body loses ~3.5× more, and that DIFFERENTIAL is what needed explaining. Swapping only the solve formulation to `GreenReconstruction` — which transmits the shed wake as a reconstructed potential trace instead of the default `VelocityThroughSources` equivalent-source proxy σ=−u_wake·n — removes **93% of the differential at η=0.0625** (16.70→1.10 pp) and **all of its η-dependence** (Dirichlet KJ gap flat at 4–6% across the ladder vs 6.6–20.3%). So the Dirichlet arm's η-sensitivity is substantially an artifact of the default wake→body transfer scheme, not physics. **Also eliminated:** wake rollup (T2 `freestream_convection=true` makes the differential LARGER, 16.70→20.85 pp — wake shape was masking, not causing), the strip/wake-row-1 strength jump (T3, ~5e−4 of Γ, three orders too small and anti-correlated with the gap), `Das` direction/drift (measured exactly 0), and wake→control-point evaluation (both formulations use zero-offset centroid control points; verified identical to the bit). Earlier eliminated: flat tip caps (3.9%) and Dirichlet G regularization (inert 1e-8…1e-4). **Residual ~15% settled-CL floor is force reconstruction, not formulation** — battery I's mesh ladder takes the steady pressure gap 13.4%→1.4% at 480→1920 cells and collapses the `:tri`/`:tri_robust` spread 7.2pp→0.35pp. T4 (operator-error and closure-identity tests) NOT run — gated on user approval. Combined η+mesh test NOT run — deliberate. ∇μ/oracle choice deferred per user. Gate record untouched and re-verified byte-identical. Detail in Phase 4 2026-07-30 sections. **Battery II(b) (mesh ladder × formulation, 3 rungs 480/1920/4320, `KUTTAV1_DIAG2_BATTERIES=mesh`) then MEASURED the residual floor** that Green leaves. The settled-CL ratio factorizes exactly into circulation × force-reconstruction (identity residual 1.1e−16); under Green the reconstruction factor equals the formulation-independent STEADY semi-infinite comparator to **0.110% / 0.040% / 0.034%** across the three rungs, while VTS departs by 4.08% / 2.56% / 1.80% — i.e. the default transfer scheme injects a spurious reconstruction error on top of its circulation error. Green's settled gap collapses onto its KJ gap (15.79%→4.89%→4.15% vs KJ 5.58%→5.22%→4.42%), so the floor was force reconstruction, now measured rather than inferred. **Open residue: the CIRCULATION half is NOT converged** — Green's KJ gap is still falling (5.58→4.42%) and remains ~3.7× its own steady anchor (3.00→1.75→1.19%) at 4440 cells. Green's settled gap at the two fine rungs (4.89%, 4.15%) is below the 5% criterion; **reported only — gate record untouched and re-verified byte-identical, V1 still blocked**, and a pass on a still-moving number would not be a pass. |
| `016_surface_vorticity_particle_shedding.md` | Gated design of an opt-in smooth surface-vorticity conversion for `PanelParticleWake`, retaining physical root/tip closure and the unchanged legacy edge-jump default. | [ ] | [ ] | [ ] | **Phase 2 APPROVED 2026-08-01 (as amended); Phase 3 IN PROGRESS, Stage 0 done.** Clear-context review against live source produced Phase 2 §13: **ghost-storage Option B selected** — conversion runs *before* the row shift (`simulate.jl:1229`), so the "ghost" is the pre-shift final active row read in place; the drafted `N+2` node rows, explicit `capacity` field, and ~20-site ghost-visibility exclusion are all unnecessary, and only the not-yet-shed row 1 is staged, only when `nwakerows==1` (Option A recorded verbatim; fallback trigger defined). Two corrections: wake serializers live in `FLOWPanel_replay.jl` not `FLOWPanel_metadata.jl` (and `schema_version` is written but never read, so §9.1 must carry its own version key); and `_deserialize_wake_shedding`'s unknown-tag `@warn`+`NoShed()` fallback (`replay.jl:518`) silently changes physics and must not be reused for conversion decoding. **Stages 0–2 DONE, 197/197 `Free Wakes`.** S0 closed the pre-existing gap that **nothing pinned `_convert_to_particles!` directly**: new `make_conversion_fixture` (writes panel-wake rows directly, **no time stepping**) + golden reference `test/data/legacy_wake_conversion_reference.jl` over `wraps × nwakerows∈{1,2,3}`, compared by **exact equality** — and it still passes bit-identically after the S1 refactor, which is the whole point. S1: `AbstractPanelParticleConversion` / `LegacyEdgeJumpConversion` (exact default) / `SurfaceVorticityConversion`, a `DefaultWakeSheddingMethod` sentinel distinguishing "said nothing" from "explicitly asked for the default", 4 new `PanelParticleWake` fields, dispatch on `wake.conversion`. S2: simulation-free reconstruction core — deterministic tangent basis, scaled-SVD rank-2/1/0 gradient, `κ=-n×∇ŝμ̂` sign, bilinear position/derivatives/normal, subdivision counts, 2×2 Gauss subcell area, panel validation, typed `WakeGeometryError`. **S3 mechanics verified:** ghost = active row N (nodes N..N+1, strength N); upstream = row N−1 for N≥2 (nothing staged); for N=1 the staged sample is centroid of node row 1 (the current TE+Das line) with strength `_get_wakestrength_mu` ⇒ **`system` must be threaded into the smooth conversion entry point**. Remaining: S3 transaction/deposition/handoff → S4 persistence (in `FLOWPanel_replay.jl`) → **S4b static sheet-vs-particle equivalence (user-requested: induced velocity vs standoff, circulation/impulse moments, legacy-vs-smooth near field, internal-edge cancellation — all without timestepping)** → S5 diagnostics. **Gate caveat:** `runtests_unit_pitching_wing_exp.jl` aborts on a missing data asset (`data/pitching_wing_exp/`, absent from repo — pre-existing, unrelated), stopping `runtests.jl` at line 21; lines 22–27 run separately and pass. `julia` exits 0 even on that load error, so judge the gate by log content. Detail in the item's phase_02 §13 and phase_03 docs. |
| `017_sheet_particle_representation_equivalence.md` | Wing sheet-to-particle equivalence map and mechanism attribution under controlled σ, spacing, and handoff distance; rotor transfer deferred. | [x] | [ ] | [ ] | **TECHNICALLY COMPLETE 2026-07-30.** Fixed-overlap legacy shedding is admissible in 14/15 cells: use σ/c≤0.6 over d/c=0.25–4, or d/c≥1 at σ/c≈1.2. The sole near-body chord-scale-core cell fails (ΔCL +2.42%, ΔΓ 2.75%). Frozen/dynamic mismatch is strength-evolution/M3a-dominated; matched-geometry M3b is <0.05 pp. Moment target, transfer variables, startup transient, and analytic rotor cost are complete; Phase D remains deferred. |
| `018_dji9443_hover_convergence_campaign.md` | Publishable hover convergence campaign: converge physical `Das`, timestep (pinned physical units), particle σ (h faster than σ), shedding distance (criterion + spot-check), truncation, mesh span+chord, Green Δ, merging null — into one converged CT + Γ(r/R) (TE μ-jump) with a closed error budget. | [ ] | [ ] | [ ] | **CAMPAIGN LIVE; Phases 0–2 CLOSED, 3/4/5 executing (2026-08-01).** P0: pinned NT=72 → 0.06852, −3.9% vs NT=36 ⇒ dt NOT converged (naive-NT=72's +2.9% was the Das confound) ⇒ Phase 3 full ladder incl. NT=144. **P1 baseline B0 = 0.07170, CI [0.07164,0.07173]** (revs 15–29, M1 PASS); restart chaining validated; cost model 9.0 h/(σ/c). P2 Das ladder 0.205/0.41/0.82/1.64c → 0.07006/0.07187/0.07230/0.07084 — top rung NON-MONOTONE, no pair passes M1+M2 ⇒ no-plateau branch: **Das\* = 0.41c PROVISIONAL**, re-test at σ=L1. **P5 (ruling 11: nwakerows is a modeling choice, not a convergence axis — converge shedding + Das instead): N-axis BRACKETED — N=1 (d=0.6σ) FAILS (−0.75%, ε_Γ 2.77%), N=2 (1.2σ) PASSES (+0.01%, 0.35%) ⇒ adopt N=2**, exactly the clearance behavior predicted (body inside the regularized σ region); N=1 may become admissible at finer σ (d/σ=1.38 at L1) — staged. Hub-radius mesh variant IMPLEMENTED (ceiling 0.15R) with 3 runs in flight to test whether near-axis particles drive the N=1 failure. T4 FMM discriminator PASSED; campaign scatter ~5× tighter than legacy. 6 jobs running (nt72, L1 cold+warm, 3 hub). Entry point: item's "Current status and next actions". |
