# 2026-07-02 overlap execution log

Started: 2026-07-02 09:30:45 MDT

## Setup

- Read `CLAUDE.md`, `plans/20260702_overlap.md`, and the relevant
  `BRAINSTORM/008_particle_overlap_vorticity_evolution.md` sections.
- `git status --short` showed an already dirty worktree with existing source
  changes and many generated/untracked data artifacts. I will keep edits scoped
  to overlap diagnostics/logging and will not clean or revert unrelated files.
- Confirmed from `examples/rotor_hover_pressure_comparison.jl`:
  `RPM=6000`, `nt=36`, so `dt = 60 / RPM / nt = 1/3600 s`.
- Confirmed the run's default relaxation path is corrected Pedrizzetti via
  `pnl.FLOWVPM.relaxation_correctedpedrizzetti`, with env-overridable
  `RELAX_RLXF`.

## Implementation notes

- Extended `examples/particle_overlap_residual.jl` so `load_state` also returns
  optional `vol`, `C`, and `SFS` fields needed by the Galerkin midpoint RHS and
  SFS-on replay metrics.
- Added shared helper module `examples/particle_overlap_galerkin.jl`.
- Added subsystem driver `examples/particle_overlap_subsystem_cond.jl`.
- First smoke command:
  `REGION=tipvortex STEP=350 NP_TARGET=300 julia --project=. -t auto examples/particle_overlap_subsystem_cond.jl`
  loaded `np=39471`, selected `seed=39113`, but the buffer sanity check exposed
  a near-global buffer: interior `300`, buffer `25831`, `kind=effective_tau`.
  I interrupted the run before the dense solve and added `BUFFER_MAX_FACTOR`
  capping so the subsystem ladder remains local and interpretable.
- Found that the saved VTP point-data key `vol` exists but is all zeros for
  step 350. `circulation` is signed and not a volume proxy. Added a fallback
  quadrature weight for the Galerkin midpoint rule:
  `w_i = 1 / Σ_j ζ_{σ_i}(X_i-X_j)`, using the same cutoff. This is now the
  active weighting for the outputs below.

## Part 1: subsystem conditioning, step 350

Commands executed:

- `REGION=tipvortex STEP=350 NP_TARGET=300 julia --project=. -t auto examples/particle_overlap_subsystem_cond.jl`
- `REGION=oldwake STEP=350 NP_TARGET=300 julia --project=. -t auto examples/particle_overlap_subsystem_cond.jl`
- Earlier `NP_TARGET=1000` runs were also executed before the zero-`vol`
  discovery; their spectra remain valid, but their RHS/solve metrics should be
  regenerated before use.

300-particle smoke results, with 300 capped buffer particles:

| region | min eig | cond | Jacobi cond | interior min eig | solve λ | buffered r_G | correction size |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| tipvortex | -6.63e-12 | -6.55e16 | -4.22e16 | -1.32e-12 | 1.00e-12 | 1.71e-10 | 1.07e7 |
| oldwake | 1.11e-11 | 5.05e16 | 1.46e16 | 1.45e-9 | 0 | 9.95e-11 | 8.58e6 |

Interpretation: the analytic Gram is symmetric, but these sub-clouds are
numerically rank-deficient at machine precision. The dense solve can drive the
linear residual to near zero, but the correction is enormous relative to the
isolated update, so the explicit unregularized subsystem solve is not a robust
update direction.

Output files:

- `data/overlap_subsystem_cond/rotor_hover_pressure_comparison_step350_tipvortex_np300.csv`
- `data/overlap_subsystem_cond/rotor_hover_pressure_comparison_step350_oldwake_np300.csv`

## Part 2: full-scale matrix-free PCG, step 350

Commands executed:

- `RUN_NAME=rotor_hover_pressure_comparison STEPS=350 CUTOFF_FACTOR=4 LAMBDA=0 INCLUDE_SFS=false MAXITER=3 julia --project=. -t auto examples/particle_overlap_galerkin_cg_diag.jl`
- `RUN_NAME=rotor_hover_pressure_comparison STEPS=350 CUTOFF_FACTOR=4 LAMBDA=0 INCLUDE_SFS=false MAXITER=20 julia --project=. -t auto examples/particle_overlap_galerkin_cg_diag.jl`
- `RUN_NAME=rotor_hover_pressure_comparison STEPS=350 CUTOFF_FACTOR=4 LAMBDA=1e-6 INCLUDE_SFS=false MAXITER=20 julia --project=. -t auto examples/particle_overlap_galerkin_cg_diag.jl`

Results:

- Initial full-scale Galerkin residual, SFS off: `r_G(Γdot0)=0.4504575`.
- `λ=0`, 3 PCG iterations: final `r_G=0.192614`, monotone, correction size
  `0.7836`.
- `λ=0`, 20 PCG iterations: final `r_G=0.0749947`, non-monotone history,
  correction size `4.2149`.
- `λ=1e-6`, 20 PCG iterations: numerically identical to `λ=0` at reported
  precision; final `r_G=0.0749947`, non-monotone, correction size `4.2149`.

Interpretation: matrix-free PCG materially reduces the Galerkin residual
(`~6x` by 20 iterations), but the history violates the monotone-residual
robustness criterion at the tested λ, and the correction is large.

## Part 3a: Pedrizzetti vs Galerkin residual, step 350

Command executed:

- `RUN_NAME=rotor_hover_pressure_comparison STEPS=350 RLXF_GRID=0,0.06,0.3,0.6,1.0 CUTOFF_FACTOR=4 LAMBDA=0 julia --project=. -t auto examples/particle_pedrizzetti_vs_galerkin.jl`

SFS-on baseline: `r_G=0.5640379`.

| variant | rlxf | direct r_G | optimal-scaled r_G | correction size |
| --- | ---: | ---: | ---: | ---: |
| vanilla | 0.00 | 0.5621 | 0.5573 | 0.0043 |
| vanilla | 0.06 | 0.5936 | 0.5545 | 0.3502 |
| vanilla | 0.30 | 1.5615 | 0.5549 | 1.7480 |
| vanilla | 0.60 | 3.0678 | 0.5549 | 3.4953 |
| vanilla | 1.00 | 5.1240 | 0.5549 | 5.8250 |
| corrected | 0.00 | 0.5621 | 0.5573 | 0.0043 |
| corrected | 0.06 | 0.5766 | 0.5484 | 0.3318 |
| corrected | 0.30 | 1.5537 | 0.5507 | 1.7315 |
| corrected | 0.60 | 3.1759 | 0.5540 | 3.5849 |
| corrected | 1.00 | 5.1240 | 0.5549 | 5.8250 |

Interpretation: direct Pedrizzetti relaxation moves away from the Galerkin
equations for practical `rlxf` values. The direction has only a weak
best-scaled benefit, bottoming around `r_G≈0.548`, far above the PCG result
`0.075`.

Output file:

- `data/pedrizzetti_vs_galerkin/rotor_hover_pressure_comparison_summary.csv`
